--- a/net/minecraft/world/entity/ai/attributes/DefaultAttributes.java
+++ b/net/minecraft/world/entity/ai/attributes/DefaultAttributes.java
@@ -93,7 +_,7 @@
 
 public class DefaultAttributes {
     private static final Logger LOGGER = LogUtils.getLogger();
-    private static final Map<EntityType<? extends LivingEntity>, AttributeSupplier> SUPPLIERS = ImmutableMap.<EntityType<? extends LivingEntity>, AttributeSupplier>builder()
+    private static final Map<EntityType<? extends LivingEntity>, AttributeSupplier> SUPPLIERS = new java.util.HashMap<>(ImmutableMap.<EntityType<? extends LivingEntity>, AttributeSupplier>builder() // Kiterino - Injecting custom entities
         .put(EntityTypes.ALLAY, Allay.createAttributes().build())
         .put(EntityTypes.ARMADILLO, Armadillo.createAttributes().build())
         .put(EntityTypes.ARMOR_STAND, ArmorStand.createAttributes().build())
@@ -187,7 +_,17 @@
         .put(EntityTypes.ZOMBIE_NAUTILUS, ZombieNautilus.createAttributes().build())
         .put(EntityTypes.ZOMBIE_VILLAGER, Zombie.createAttributes().build())
         .put(EntityTypes.ZOMBIFIED_PIGLIN, ZombifiedPiglin.createAttributes().build())
-        .build();
+        .build()); // Kiterino - Injecting custom entities
+
+    // Kiterino start - Injecting custom entities
+    static {
+        me.sosedik.kiterino.util.KiterinoBootstrapEntityTypeInjectorImpl.attributes();
+    }
+
+    public static void register(EntityType<? extends LivingEntity> entityType, AttributeSupplier supplier) {
+        SUPPLIERS.put(entityType, supplier);
+    }
+    // Kiterino end - Injecting custom entities
 
     public static AttributeSupplier getSupplier(final EntityType<? extends LivingEntity> type) {
         return SUPPLIERS.get(type);
