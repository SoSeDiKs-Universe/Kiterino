--- a/net/minecraft/world/entity/LivingEntity.java
+++ b/net/minecraft/world/entity/LivingEntity.java
@@ -2603,6 +_,7 @@
                 ClientboundAnimatePacket clientboundAnimatePacket = new ClientboundAnimatePacket(this, hand == InteractionHand.MAIN_HAND ? 0 : 3);
                 ServerChunkCache chunkSource = ((ServerLevel)this.level()).getChunkSource();
                 if (updateSelf) {
+                    if (this instanceof ServerPlayer serverPlayer && new me.sosedik.kiterino.event.player.PlayerArmSwingFromServerEvent(serverPlayer.getBukkitEntity(), hand == InteractionHand.MAIN_HAND ? org.bukkit.inventory.EquipmentSlot.HAND : org.bukkit.inventory.EquipmentSlot.OFF_HAND).callEvent()) // Kiterino - Add PlayerArmSwingFromServerEvent
                     chunkSource.broadcastAndSend(this, clientboundAnimatePacket);
                 } else {
                     chunkSource.broadcast(this, clientboundAnimatePacket);
@@ -3637,8 +_,15 @@
                 }
 
                 if (flag) {
-                    if (getRider() == null || !this.isControllable()) // Purpur - ignore mobs which are uncontrollable or without rider - API for any mob to burn daylight
-                        this.igniteForSeconds(8.0F);
+                    if (getRider() == null || !this.isControllable() || getRider().isInvisible()) // Purpur - ignore mobs which are uncontrollable or without rider - API for any mob to burn daylight // Kiterino - Allow burning ridables with invisible rider
+                    // Kiterino start - Add EntitySunburnEvent
+                    {
+                        var event = new me.sosedik.kiterino.event.entity.EntitySunburnEvent(getBukkitLivingEntity(), 8F);
+                        if (event.callEvent()) {
+                            this.igniteForSeconds(event.getBurnDuration());
+                        }
+                    }
+                    // Kiterino end - Add EntitySunburnEvent
                 }
             }
         }
