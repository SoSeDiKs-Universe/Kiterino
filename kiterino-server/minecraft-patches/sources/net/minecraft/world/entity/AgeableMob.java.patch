--- a/net/minecraft/world/entity/AgeableMob.java
+++ b/net/minecraft/world/entity/AgeableMob.java
@@ -157,6 +_,7 @@
         if (oldAge < 0 && newAge >= 0 || oldAge >= 0 && newAge < 0) {
             this.entityData.set(DATA_BABY_ID, newAge < 0);
             this.ageBoundaryReached();
+            new me.sosedik.kiterino.event.entity.MaybeAsyncEntityChangeAgeEvent((org.bukkit.entity.Ageable) getBukkitEntity()).callEvent(); // Kiterino - Add EntityChangeAgeEvent
         }
     }
 
