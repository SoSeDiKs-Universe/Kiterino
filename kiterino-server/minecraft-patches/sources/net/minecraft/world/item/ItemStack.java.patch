--- a/net/minecraft/world/item/ItemStack.java
+++ b/net/minecraft/world/item/ItemStack.java
@@ -141,6 +_,15 @@
                 if (false && !dataComponentPatch.isEmpty()) { // Paper - This is no longer needed with raw NBT being handled in metadata
                     org.bukkit.craftbukkit.inventory.CraftItemStack.setItemMeta(stack, org.bukkit.craftbukkit.inventory.CraftItemStack.getItemMeta(stack));
                 }
+                // Kiterino start - Prevent creative from overriding items
+                if (me.sosedik.kiterino.KiterinoConfig.preventCreativeItemOverride) {
+                    net.minecraft.world.item.component.CustomData customData = stack.components.get(DataComponents.CUSTOM_DATA);
+                    if (customData != null && customData.contains("kiterino_og_item")) {
+                        Tag itemTag = customData.getUnsafe().get("kiterino_og_item");
+                        stack = parse(buffer.registryAccess(), itemTag).orElse(stack);
+                    }
+                }
+                // Kiterino end - Prevent creative from overriding items
                 return stack;
                 // CraftBukkit end
             }
@@ -156,6 +_,19 @@
                 // Spigot start - filter
                 // value = value.copy();
                 // CraftItemStack.setItemMeta(value, CraftItemStack.getItemMeta(value)); // Paper - This is no longer with raw NBT being handled in metadata
+                // Kiterino start - Prevent creative from overriding items
+                net.minecraft.world.item.component.CustomData customData = null;
+                if (me.sosedik.kiterino.KiterinoConfig.preventCreativeItemOverride && !value.isEmpty() && me.sosedik.kiterino.modifier.item.ItemModifiersHandlerImpl.modifiers.isEmpty()) {
+                    customData = value.get(DataComponents.CUSTOM_DATA);
+                    if (customData == null) {
+                        customData = net.minecraft.world.item.component.CustomData.of(new CompoundTag());
+                        customData.getUnsafe().put("kiterino_og_item", value.saveOptional(buffer.registryAccess()));
+                        value.set(DataComponents.CUSTOM_DATA, customData);
+                    } else {
+                        customData.getUnsafe().put("kiterino_og_item", value.saveOptional(buffer.registryAccess()));
+                    }
+                }
+                // Kiterino end - Prevent creative from overriding items
                 // Paper start - adventure; conditionally render translatable components
                 boolean prev = net.minecraft.network.chat.ComponentSerialization.DONT_RENDER_TRANSLATABLES.get();
                 try (final io.papermc.paper.util.SafeAutoClosable ignored = io.papermc.paper.util.ItemObfuscationSession.withContext(c -> c.itemStack(value))) { // pass the itemstack as context to the obfuscation session
@@ -163,6 +_,15 @@
                 DataComponentPatch.STREAM_CODEC.encode(buffer, value.components.asPatch());
                 } finally {
                     net.minecraft.network.chat.ComponentSerialization.DONT_RENDER_TRANSLATABLES.set(prev);
+                    // Kiterino start - Prevent creative from overriding items
+                    if (customData != null) {
+                        if (customData.getUnsafe().size() == 1) {
+                            value.remove(DataComponents.CUSTOM_DATA);
+                        } else {
+                            customData.getUnsafe().remove("kiterino_og_item");
+                        }
+                    }
+                    // Kiterino end - Prevent creative from overriding items
                 }
                 // Paper end - adventure; conditionally render translatable components
             }
