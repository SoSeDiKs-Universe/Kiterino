From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: SoSeDiK <mrsosedik@gmail.com>
Date: Wed, 22 Oct 2025 14:53:13 +0300
Subject: [PATCH] Correctly handle insta break destroy progress


diff --git a/net/minecraft/world/level/block/state/BlockBehaviour.java b/net/minecraft/world/level/block/state/BlockBehaviour.java
index 279f1936f631ad61c89a14afddb84aecba4cd2f4..d27c49fbcb33cf21aa900d5d5e4efa1d136cbcb4 100644
--- a/net/minecraft/world/level/block/state/BlockBehaviour.java
+++ b/net/minecraft/world/level/block/state/BlockBehaviour.java
@@ -358,6 +358,11 @@ public abstract class BlockBehaviour implements FeatureElement {
 
     protected float getDestroyProgress(final BlockState state, final Player player, final BlockGetter level, final BlockPos pos) {
         float destroySpeed = state.getDestroySpeed(level, pos);
+        // Kiterino start - Correctly handle insta break destroy progress
+        if (destroySpeed == 0F) {
+            return 1F;
+        }
+        // Kiterino end - Correctly handle insta break destroy progress
         if (destroySpeed == -1.0F) {
             return 0.0F;
         }
