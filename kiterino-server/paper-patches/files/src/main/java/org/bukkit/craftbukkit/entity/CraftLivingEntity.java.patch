--- a/src/main/java/org/bukkit/craftbukkit/entity/CraftLivingEntity.java
+++ b/src/main/java/org/bukkit/craftbukkit/entity/CraftLivingEntity.java
@@ -1111,4 +_,30 @@
         this.getHandle().setShouldBurnInDay(shouldBurnInDay);
     }
     // Purpur end - API for any mob to burn daylight
+
+    // Kiterino start - Expose checkBedExists
+    @Override
+    public boolean bedExists() {
+        return this.getHandle().checkBedExists();
+    }
+    // Kiterino end - Expose checkBedExists
+
+    // Kiterino start - Allow laying at will
+    @Override
+    public void lay(Location sleepLocation, Location bedLocation) {
+        this.getHandle().setPose(net.minecraft.world.entity.Pose.SLEEPING);
+        this.getHandle().setSleepingPos(new net.minecraft.core.BlockPos(bedLocation.getBlockX(), bedLocation.getBlockY(), bedLocation.getBlockZ()));
+        this.getHandle().setPos(sleepLocation.getX(), sleepLocation.getY(), sleepLocation.getZ());
+        this.getHandle().setDeltaMovement(Vec3.ZERO);
+        this.getHandle().hasImpulse = true;
+        if (this.getHandle() instanceof net.minecraft.server.level.ServerPlayer player && player.connection != null) {
+            // Sync player from bed to actual sleep loc
+            this.taskScheduler.schedule(entity -> {
+                if (player.connection != null) {
+                    player.connection.teleport(sleepLocation);
+                }
+            }, entity -> {}, 4L);
+        }
+    }
+    // Kiterino end - Allow laying at will
 }
