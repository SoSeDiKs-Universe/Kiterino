--- a/src/main/java/org/bukkit/craftbukkit/inventory/CraftRecipe.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/CraftRecipe.java
@@ -77,7 +_,11 @@
                 choices.add(CraftItemStack.asBukkitCopy(i));
             }
 
-            return new RecipeChoice.ExactChoice(choices);
+            // Kiterino start - Add predicate support for the recipe book
+            var recipeChoice = new RecipeChoice.ExactChoice(choices);
+            if (list.predicate != null) recipeChoice.setPredicate(list.predicate);
+            return recipeChoice;
+            // Kiterino end - Add predicate support for the recipe book
         } else {
             final RegistryKeySet<ItemType> itemTypes = PaperRegistrySets.convertToApi(RegistryKey.ITEM, list.values);
             return RecipeChoice.itemType(itemTypes);
