From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: SoSeDiK <mrsosedik@gmail.com>
Date: Wed, 11 Jan 2023 20:01:10 +0200
Subject: [PATCH] Mob possessing mechanic compatibility


diff --git a/src/main/java/net/minecraft/world/entity/Entity.java b/src/main/java/net/minecraft/world/entity/Entity.java
index fc1314cf14cfa47eeaf4d08a84e98422751a7226..3539e7e065db1bfe4c566dc34c404a53f6ceb8ea 100644
--- a/src/main/java/net/minecraft/world/entity/Entity.java
+++ b/src/main/java/net/minecraft/world/entity/Entity.java
@@ -1441,7 +1441,7 @@ public abstract class Entity implements Nameable, EntityAccess, CommandSource {
         final Level world = this.level;
         // Kiterino start - include vehicle's height in collision
         AABB currBoundingBox = this.getBoundingBox();
-        if (getVehicle() != null) {
+        if (getVehicle() != null && !isInvisible() && !isInvulnerable()) {
             currBoundingBox = new AABB(currBoundingBox.minX, getVehicle().getBoundingBox().minY + 0.1, currBoundingBox.minZ, currBoundingBox.maxX, currBoundingBox.maxY, currBoundingBox.maxZ);
         }
         // Kiterino end
@@ -1503,6 +1503,9 @@ public abstract class Entity implements Nameable, EntityAccess, CommandSource {
             }
             // Kiterino start - include vehicle's height in collision
             for (Entity passenger : getPassengers()) {
+                if (passenger.isInvisible() && passenger.isInvulnerable()) {
+                    continue;
+                }
                 BlockState state = world.getBlockState(new BlockPos(passenger.position().add(0, passenger.getBbHeight(), 0).add(movement.normalize())));
                 if (!state.is(BlockTags.LEAVES)) {
                     Vec3 passengerCollision = passenger.collide(movement);
